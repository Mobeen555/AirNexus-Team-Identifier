# Put this in Streamlit Cloud -> App -> Settings -> Secrets

APP_BASE_URL = "https://YOUR-APP-NAME.streamlit.app"
ADMIN_PASSWORD = "CHANGE-THIS-TO-A-STRONG-PASSWORD"

# Optional but strongly recommended:
# Generate a Fernet key in Python:
# from cryptography.fernet import Fernet
# print(Fernet.generate_key().decode())
APP_ENCRYPTION_KEY = "PASTE-FERNET-KEY-HERE"

# Optional additional secret used for the rotating live verification code:
LIVE_SECURITY_SECRET = "CHANGE-THIS-TO-A-LONG-RANDOM-SECRET"

# Optional Google/Microsoft Authenticator MFA:
# Generate a base32 secret:
# import pyotp
# print(pyotp.random_base32())
ADMIN_TOTP_SECRET = ""
